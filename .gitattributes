# Shell scripts must always use LF line endings. They run inside Linux
# containers (e.g. vault/init/init.sh is mounted into the vault-init
# container), and CRLF line endings break the /bin/sh interpreter.
*.sh text eol=lf
